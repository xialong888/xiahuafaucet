# 🎠 产品轮播功能 - 视觉指南

## 界面布局示意图

```
┌─────────────────────────────────────────────────┐
│  产品卡片 (Product Card)                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌───────────────────────────────────────────┐  │
│  │                                           │  │
│  │  ◀ (当悬停时显示)    [产品图片]    ▶     │  │
│  │  LEFT ARROW          IMAGE          RIGHT │  │
│  │  BUTTON              CAROUSEL       BUTTON  │  │
│  │                                           │  │
│  │  ● ● ● ○ ○ ○  (左下)  3/6 (右下)     │  │
│  │  THUMBNAIL            IMAGE               │  │
│  │  INDICATORS          COUNTER              │  │
│  │                                           │  │
│  │              XH-ABS-101 (右上)            │  │
│  │              MODEL LABEL                  │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  产品名称：Patented Premium Chrome ABS ...     │
│                                                 │
│  材料：Virgin Heavy ABS (Patent Grade)          │
│  表面处理：9-Class Multi-layer Electroplated    │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │   查看规格表  ▶                         │   │
│  │   (View Specifications)                 │   │
│  └─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

---

## 📍 各部分详细说明

### 1️⃣ **导航箭头按钮**

#### 上一张按钮（Previous）
```
┌──────────────────────────────────┐
│  默认状态（未悬停）              │
│  透明，不可见                    │
│                                  │
│  悬停状态                        │
│  ◀ ← 显示，半透明深灰背景       │
│  hover 时背景加深                │
│  点击切换到上一张图片            │
└──────────────────────────────────┘

样式细节：
- 大小：18px 图标 + 8px padding = ~34px 圆形按钮
- 位置：图片左边 8px，垂直居中
- 背景：bg-slate-900/70 (70% 不透明)
- Hover：bg-slate-900 (100% 不透明)
- 动画：opacity-0 → opacity-100 (duration-300)
```

#### 下一张按钮（Next）
```
同上，位置在图片右边 8px
```

### 2️⃣ **图片计数器**

```
位置：右下角 (bottom-3, right-3)
格式：当前索引 / 总数
示例：
  1/6 (第 1 张，共 6 张)
  3/6 (第 3 张，共 6 张)
  6/6 (第 6 张，共 6 张)

样式：
- 背景：深灰色半透明 + 模糊效果
- 字体：10px，粗体，白色
- 内边距：px-2.5 py-1
- 圆角：rounded-md
```

### 3️⃣ **缩略图指示点（Thumbnail Dots）**

```
位置：左下角 (bottom-3, left-3)
排列：水平排列，可换行

状态变化：

当前选中的点：
  ●  <- 白色 (bg-white)，宽一点 (w-3)

未选中的点：
  ○  <- 半透明白色 (bg-white/50)，小圆点 (w-1.5)
  
Hover 未选中的点：
  ◐  <- 加深 (bg-white/75)，提示可点击

例子（6 张图片，当前第 3 张）：
  ○ ○ ● ○ ○ ○
      ↑ 当前选中

操作：
- 点击任意点可直接跳转到对应的图片
- 支持在非 hover 状态下点击
```

### 4️⃣ **模型标签**

```
位置：右上角 (top-3, right-3)
内容：产品型号（例：XH-ABS-101）
格式：大写、粗体、字间距宽

样式：
- 背景：深灰色半透明 + 模糊
- 字体：11px, font-black, uppercase
- 内边距：px-2.5 py-1
- 字间距：tracking-widest
- 颜色：白色
```

---

## 🎬 交互流程图

### 场景 1：使用箭头按钮浏览

```
用户悬停产品卡片
  ↓
箭头按钮显示（逐渐不透明）
  ↓
用户点击右箭头 ▶
  ↓
carouselIndex[productId] += 1
  ↓
图片从第 2 张切换到第 3 张（500ms 过渡）
  ↓
计数器：2/6 → 3/6
  ↓
缩略点：第 2 个 ○ → 第 3 个 ● 变白变宽
  ↓
用户继续点击...
  ↓
最后一张 (6/6) → 点击右箭头 → 回到第一张 (1/6)  [循环]
```

### 场景 2：使用缩略点直接跳转

```
用户看到缩略点
  ↓
点击第 5 个点 ○ (无论悬停与否)
  ↓
setProductCarouselIndex(productId, 4)  // 索引从 0 开始
  ↓
图片直接切换到第 5 张 (500ms 过渡)
  ↓
计数器：3/6 → 5/6
  ↓
缩略点：第 3 个 ● → 第 5 个 ●
```

### 场景 3：移动到不同的产品卡片

```
用户在产品 1 看第 3 张 (3/6)
  ↓
用户移动到产品 2 卡片
  ↓
产品 2 显示它自己的 0 索引图片 (1/6)
  ↓
产品 1 的状态 (3/6) 保留在内存中
  ↓
用户回到产品 1
  ↓
产品 1 恢复显示第 3 张 (3/6)  [状态保留]
```

---

## 📐 响应式布局

### 桌面版（Desktop）
```
4 列网格布局
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│ Prod │ │ Prod │ │ Prod │ │ Prod │
│  1   │ │  2   │ │  3   │ │  4   │
└──────┘ └──────┘ └──────┘ └──────┘

卡片宽度：按网格均匀分配
轮播组件：全尺寸（4:3 宽高比）
箭头大小：18px
点的大小：2-6px
```

### 平板（Tablet）
```
2 列网格布局
┌──────────────┐ ┌──────────────┐
│   Prod 1     │ │   Prod 2     │
├──────────────┤ ├──────────────┤
│   Prod 3     │ │   Prod 4     │
└──────────────┘ └──────────────┘

卡片宽度：50% - gaps
轮播组件：适应较大的视图
```

### 手机（Mobile）
```
1 列全宽布局
┌────────────────────────┐
│     Prod 1             │
├────────────────────────┤
│     Prod 2             │
├────────────────────────┤
│     Prod 3             │
├────────────────────────┤
│     Prod 4             │
└────────────────────────┘

卡片宽度：100% - padding
轮播组件：堆栈显示
箭头仍可见且易点击
```

---

## 🎨 颜色/样式参考

### 深色主题（Tailwind）
```
按钮背景：
  默认：bg-slate-900/70 (rgba(15, 23, 42, 0.7))
  Hover：bg-slate-900 (rgb(15, 23, 42))

文本：
  全部为白色 (text-white)

背景模糊：
  backdrop-blur (CSS filter blur)

指示点：
  当前：bg-white (w-3)
  未选：bg-white/50 (w-1.5)
  Hover：bg-white/75
```

---

## ⚡ 动画时序

```
图片过渡：
  transition-transform duration-500
  效果：hover:scale-105 (放大 5%)
  时间：500ms

箭头显示/隐藏：
  opacity-0 → opacity-100
  duration-300 (300ms)
  timing：smooth (cubic-bezier default)

点击按钮：
  active 时：no visible feedback
  建议扩展：scale-95 on active

缩略点选中：
  w-1.5 → w-3
  background 颜色变化
  transition-all (所有属性)
```

---

## 🔄 数据流向

```
产品数据结构：
┌─────────────────────────────────┐
│ Product Object                  │
├─────────────────────────────────┤
│ id: 1                           │
│ model: "XH-ABS-101"             │
│ nameMap: { en, ru, pt, hi, zh } │
│ images: [                       │
│   "/assets/products/xh-1.jpg",  │
│   "/assets/products/xh-2.jpg",  │
│   ...  (总共 6 张)              │
│ ]                               │
│ img: "/assets/products/xh-1.jpg"│
└─────────────────────────────────┘
         │
         ↓ 渲染时查询
┌─────────────────────────────────┐
│ carouselIndex Object            │
├─────────────────────────────────┤
│ {                               │
│   1: 2,  // 产品 1 显示第 3 张   │
│   2: 0,  // 产品 2 显示第 1 张   │
│   3: 4,  // 产品 3 显示第 5 张   │
│   4: 1,  // 产品 4 显示第 2 张   │
│ }                               │
└─────────────────────────────────┘
         │
         ↓ 获取当前索引
┌──────────────────────────────────┐
│ images[carouselIndex[productId]]  │
│ = images[2]                      │
│ = "/assets/products/xh-3.jpg"    │
└──────────────────────────────────┘
         │
         ↓ 渲染到 DOM
    <img src="..." />
```

---

## 📊 性能指标

### 每个产品卡片的资源占用

```
JavaScript：
  - carouselIndex 对象：每个产品 1 个数字 ~8 字节
  - 三个函数引用：共享的，不额外增加
  总计：极小 (~1KB 对于 4 个产品)

DOM 节点：
  - 新增：3 个按钮 + N 个缩略点 + 1 计数器
  - 每个产品：3 + 6 + 1 = 10 个新节点
  - 4 个产品：40 个新节点（可接受）

CSS：
  - 无新的样式表，全部使用 Tailwind
  - 已有样式复用：bg-slate-900, opacity, transition 等

性能：
  - 零 JavaScript 运行时开销（纯状态管理）
  - 图片加载：浏览器缓存优化（同一批图片）
  - 过渡动画：GPU 加速（transform, opacity）
```

---

## ✅ 功能完整性检查表

- [x] 轮播箭头按钮实现
- [x] 图片计数器显示
- [x] 缩略图指示点（可点击）
- [x] 循环播放（首尾相连）
- [x] 独立状态管理（多产品）
- [x] 悬停效果（箭头、图片）
- [x] 响应式设计
- [x] 可访问性（aria-label）
- [x] 向后兼容（fallback 到 p.img）
- [x] 零构建错误
- [x] 多语言支持（使用 p.nameMap[lang]）

---

## 🚀 启动方式

```bash
cd xiahuafaucet

# 开发模式（实时热重载）
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview
```

访问：`http://localhost:5173`

---

**实现完成！所有 4 个产品卡片现在都有完全交互式的图片轮播功能。** 🎉
